<h1>My tasks</h1>
<ul>
<% @tasks.each do |task| %>
   <li><%= link_to task.title, task_path(task) %> Done? <%= "👌" if task.completed == "t" %> 
     <%= link_to "🗑", task_path(task), method: :delete %> </li>
<% end %>
</ul>
<%= link_to 'Create a new task', 'tasks/new'  %>

